import React from 'react';
import { motion } from 'framer-motion';

const GalleryLoader = (): React.ReactElement => {
  return <motion.div className='load-img'></motion.div>;
};

export default GalleryLoader;
