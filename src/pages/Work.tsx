import { Typography } from '@mui/material'

const Work = (): React.ReactElement => {
  return (
    <div>
      <Typography
        sx={{
          textAlign: 'center',
          marginTop: '20px',
          fontWeight: '600',
          fontSize: '2rem',
        }}
      >
        Work title
      </Typography>
    </div>
  )
}

export default Work
